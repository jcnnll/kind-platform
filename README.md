# kind-platform

A local Kubernetes platform lab built on **KIND** for experimenting with
cluster-level configuration, platform primitives, and operational patterns.

This repo provides a reproducible environment for testing how a Kubernetes
“platform layer” behaves in a controlled local setup.
